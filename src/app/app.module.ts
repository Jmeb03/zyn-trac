import { NgModule } from '@angular/core';
import { CommonModule } from '@angular/common'; // Add this import


@NgModule({
  declarations: [
  ],
  imports: [
    CommonModule, // or BrowserModule if this is the root module
  ],
  providers: [
    
  ],
})
export class AppModule {}